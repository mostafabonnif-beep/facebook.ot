# تقرير التجربة الفعلية + ما ينقص المشروع ليعمل

تاريخ التجربة: 2026-09-12 · بيئة الاختبار: Linux sandbox (Chrome 150, ffmpeg, Python 3.10)
الملف المُختبر: `fb_youtube_uploader_v34.py` (وقتها) — النسخة الحالية: `fb_youtube_uploader_v36.py`

---

## 1) ما تم اختباره فعلياً ويعمل ✅

| المرحلة | النتيجة |
|---|---|
| تشغيل Selenium + Chrome headless | ✅ يعمل (chromedriver يُجلب تلقائياً) |
| كشف جدار تسجيل الدخول في فيسبوك | ✅ يعمل — فيسبوك يطلب دخولاً لصفحات الريلز |
| تنزيل ريل عام عبر yt-dlp | ✅ يعمل بلا دخول (62MB, av1 1080p) |
| فحص المقطع بـ ffprobe | ✅ يقرأ codec/مدة/أبعاد ويرفض المعطوب |
| تغليف ffmpeg (faststart + حذف metadata) | ✅ تحققنا: عنوان فيه اسم الصفحة اختفى تماماً، وmoov في المقدمة |
| تنقية العنوان/الوصف + إخفاء اسم الصفحة | ✅ على عنوان حقيقي مليء بالضجيج |
| العامل الكامل (سحب→تنزيل→فحص→تغليف→تنقية→حذف بعد الرفع) | ✅ يعمل من الطرف للطرف |
| منع التكرار عبر `uploaded_ids.txt` | ✅ تخطى مقطعاً مرفوعاً سابقاً |
| 49 اختبار وحدة + دخان واجهة | ✅ الكل يمر |

**وما أصلحناه أثناء التجربة:** كلمة «reactions» لم تُحذف من العنوان، فواصل `· |` كانت تتبقى في الأطراف، واسم الصفحة صار يُحذف تلقائياً أيضاً من حقل `uploader/channel` حتى لو تركتَ خانة الاسم فارغة.

---

## 2) ما ينقصه فعلياً — مطلوب منك 🔑

### أ) أساسي (بدونه لا يعمل أبداً)
1. **`client_secrets.json`** (تفويض Google) — بجانب البرنامج. الخطوات:
   - [Google Cloud Console](https://console.cloud.google.com) → مشروع جديد.
   - فعّل **YouTube Data API v3**.
   - OAuth consent screen: نوع External، وانقله إلى **"In production"**
     (إن بقي "Testing" **ينتهي التوكن خلال 7 أيام**).
   - Credentials → Create **OAuth client ID** نوع **Desktop app** → نزّل JSON وسمّه `client_secrets.json`.
2. **Python 3.11 أو أحدث** — yt-dlp أوقف دعم 3.10، ومكتبات Google ستتبعه. على جهازك ثبّت Python 3.12.

### ب) للنشر العام (مهم جداً — اكتشفناه من البحث)
3. **حصة YouTube API = 10,000 وحدة/يوم افتراضياً، ورفع الفيديو يكلف 1600 وحدة**
   → **≈ 6 فيديوهات يومياً فقط**. لو تحتاج أكثر: اطلب زيادة الحصة من Google، أو فعّل «وضع الرفع بالمتصفح» (مقترح v3.5).
4. **تدقيق التطبيق (App verification):** لو مشروع Google غير مُدقّق (Unverified)، الرفع العام
   **قد ينقفل private إجباري** مهما اخترت «عام». الحلول: قدّم تدقيق OAuth لدى Google، أو وضع الرفع بالمتصفح.

### ج) لمرحلة سحب الريلز (تأكدنا أنها تحتاجها)
5. **كوكيز فيسبوك `cookies.txt`** — فيسبوك يطلب تسجيل دخول لعرض ريلز الصفحة (تأكدنا: 0 روابط بلا كوكيز):
   - سجّل دخولك لفيسبوك في Chrome بحساب يصل للصفحات المستهدفة.
   - ثبّت إضافة **"Get cookies.txt LOCALLY"** وصدّر الكوكيز بصيغة Netscape.
   - اختر الملف من «خيارات متقدمة ← ملف كوكيز فيسبوك».
   - ملاحظة: التنزيل نفسه لا يحتاجها (الريلز العامة تنزّل بلا دخول) — لكن **الاكتشاف يحتاجها**.

### د) بيئة التشغيل على جهازك
6. **Google Chrome** مثبت (Selenium يجلب chromedriver تلقائياً — تأكدنا أنه يعمل).
7. **ffmpeg/ffprobe** على PATH (للفحص والتغليف؛ بدونه تعمل الأداة لكن بلا تحسين مقطع).
8. **إنترنت مستقر + قرص** — المقطع ≈ 60MB؛ مع «حذف بعد الرفع» الاستهلاك قليل.

---

## 3) نقاط ضعف متبقية (من التجربة — للتطوير القادم)

| المشكلة | الأثر | المقترح |
|---|---|---|
| الاكتشاف بدون كوكيز = صفر نتائج | يتوقف السحب بصمت إن انتهت الكوكيز | تنبيه أقوى + إعادة طلب كوكيز |
| السحب عبر Selenium بطيء وهشّ | يتعطل عند تغيير فيسبوك واجهتها | مسار احتياطي بلا متصفح |
| لا يوجد رفع متصفح | يتوقف عند نفاد الحصة أو قفل الخصوصية | **وضع رفع بالمتصفح (v3.5)** — أولوية |
| منع التكرار بسيط (معرّف واحد) | لا يمنع تكرار نفس المقطع بصيغة أخرى | دفتر per-media (نمط crosspost) |

---

## 4) أسرع طريق للتشغيل على جهازك (Windows)

```bat
REM 1) ثبّت Python 3.12 + Chrome + ffmpeg (على PATH)
py -3.12 -m pip install -r requirements.txt
copy config.ini.example config.ini

REM 2) ضع client_secrets.json بجانب البرنامج (من Google Cloud — Production)
REM 3) صدّر cookies.txt من فيسبوك وضعه بجانب البرنامج (اختياري لكن موصى به)
py fb_youtube_uploader_v36.py
```
ثم: أضف صفحة واحدة، اربط القناة، وجرّب **ريلاً واحداً** قبل القائمة الكبيرة.
